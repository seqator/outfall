/**
 * Публичный вход `ui/inventory` (OF-017): только DOM-рендер и вид-модель,
 * без бизнес-логики — см. заголовок `render.ts`.
 */

export * from './types';
export * from './format';
export * from './render';
