/**
 * Публичный вход слоя `game`: оркестрация — сцены (меню/локация/диалог),
 * склейка `core`/`sim` с `render`/`audio`/`ui`/`input`/`assets`. Полноценные
 * сцены появятся вместе с ядром и картой (OF-010/015); пока — каркасная
 * demo-сцена для OF-005.
 */

export * from './demo-scene';
export * from './inventory';
