#!/usr/bin/env tsx
/**
 * Заглушка: сборка атласов из `art/` в `public/atlases/` через `sharp`
 * (упаковка, палитровая квантизация PNG) — задача OF-022/OF-006. Арт-контента
 * пока нет, поэтому скрипт ничего не делает, кроме honest-сообщения.
 */

console.log(
  'atlas: пайплайн упаковки атласов — задача OF-022 (см. docs/BACKLOG.md). Пока нет источников в art/.',
);
